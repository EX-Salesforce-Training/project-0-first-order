trigger CardTrigger on Credit_Card__c (after insert) {
    switch on trigger.operationType{
        when AFTER_INSERT{
    		CreditCardTriggers.ExpiredCard(trigger.new);
        }
        when AFTER_UPDATE{
            CreditCardTriggers.ExpiredCard(trigger.new);
        }
    }
}