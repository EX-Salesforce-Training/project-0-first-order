trigger ClassRosterTrigger on Class_Roster__c (before insert) {
    switch on trigger.operationType {
        when BEFORE_INSERT {
            ClassRosterTriggerHandler.AutoAddCharge(trigger.new);
        }
    }
}